// const request = require('supertest');
// const app = require('../app');

// describe('Votes', () => {
//   it('should upvote an issue', async () => {
//     const res = await request(app).post('/api/votes').send({ issueId: 'some-id' });
//     expect(res.statusCode).toEqual(200);
//   });
// });